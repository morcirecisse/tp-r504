name: tests unitaires
on: [push]
jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python for Demo
        uses: actions/setup-python@v4
      - name: Insrtall pytezst
        run: python -m pip install pytest;
      - name: Run tests1
        run: pytest TP1/test.py
